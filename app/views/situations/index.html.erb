<p id="notice"><%= notice %></p>

<h1>Situations</h1>

<table>
  <thead>
    <tr>
      <th>Game</th>
      <th>Bat counts</th>
      <th>Inings</th>
      <th>results</th>
      <th>where_go</th>
      <th>ball_counts</th>
      <th>ball_strike</th>
      <th>rbi</th>
      <th>steal_counts</th>
      <th>score</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @situations.each do |situation| %>
      <tr>
        <td><%= situation.game_id %></td>
        <td><%= situation.bat_counts %></td>
        <td><%= situation.inings %></td>
        <td><%= situation.actness_results %></td>
        <td><%= situation.where_go %></td>
        <td><%= situation.ball_counts %></td>
        <td><%= situation.ball_strike %></td>
        <td><%= situation.rbi %></td>
        <td><%= situation.steal_counts %></td>
        <td><%= situation.score %></td>
        <td><%= link_to 'Show', situation %></td>
        <td><%= link_to 'Edit', edit_situation_path(situation) %></td>
        <td><%= link_to 'Destroy', situation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Back_to_Game', games_path %>|
<%= link_to 'Go_to_actness', actnesses_path %>

<%= javascript_pack_tag 'situations/index' %>
